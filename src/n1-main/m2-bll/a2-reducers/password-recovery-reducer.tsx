import React from 'react';

type StateType = {}
type ActionType = {
  type: ''
}

export const passwordRecoveryReducer = (state: StateType = {}, action: ActionType) => {
  switch (action.type) {
    default: {
      return state
    }
  }
};